# My inital terraform file
- this script is used to create an ec2 instance
- to run it use the follwing commands:
  ```
  terraform init
  terraform plan
  terrafrom aply

  ```
  ==> make sure to install terraform and aws cli and conifg it with your aws's access key first.
